# تنفيذ متطلبات سينماتي — 21 نقطة

هذه النسخة تحتوي على تنفيذ وتجميع التعديلات المطلوبة على واجهة التطبيق، المشغل، البحث، التصنيفات، التنزيلات والعمل دون اتصال.

## ما تم تنفيذه

1. إعادة تصميم البار السفلي كـ Film Dock زجاجي عائم مع حركة اختيار، هوية سينمائية، Safe Area واهتزاز لمسي عند التنقل، وإنزاله بصرياً قليلاً.
2. إعادة تصميم البار العلوي: شعار واسم سينماتي يميناً، وأزرار متابعة المشاهدة والتنزيلات يساراً مع Badge للتنزيلات.
3. تنزيل الترجمة مع الفيلم/الحلقة عند توفرها وحفظها محلياً للاستخدام دون إنترنت.
4. إظهار رقم الحلقة بوضوح فوق غلاف التنزيلات.
5. الترجمة الافتراضية Arabic UI بحجم 50، ومدى الحجم من 30 إلى 200.
6. إعادة بناء معاينة السحب Seek Preview بحيث تتبع موضع الإصبع/المؤشر الصحيح، مع Throttling + Cache للصور المصغرة لمنع التقطيع.
7. تغيير الجودة يحفظ موضع التشغيل الحالي ويكمل من نفس الثانية.
8. تحسين واجهة اختيار الجودة وإظهار حالة التبديل بدون إرباك المشاهدة.
9. إضافة Double Tap للتقديم/الترجيع 10 ثوانٍ مع تأثير بصري واضح.
10. إضافة أوضاع عرض الفيديو: احتواء، ملء مع قص، وتمديد.
11. إضافة زر الحلقة التالية في آخر 10 ثوانٍ مع تعبئة من اليمين إلى اليسار مرتبطة فعلياً بموضع الفيديو ثم انتقال تلقائي.
12. إضافة متصفح المواسم والحلقات داخل المشغل مع تمييز الموسم والحلقة الحالية والانتقال المباشر.
13. تحسينات إضافية للمشغل: سرعة التشغيل، تنظيم التحكمات، الاستجابة للمس، إدارة الترجمة، وتحسينات الانتقال بين المصادر.
14. توحيد لغة البارات العلوية في الرئيسية، اكتشف، البحث، التصنيفات، الممثل، المكتبة، التنزيلات، متابعة المشاهدة، المشاهدة لاحقاً والإعدادات.
15. إصلاح منطق جلب التصنيفات ومحاولة حقول/أنواع فلترة مختلفة مع اكتشاف الحالة التي يتجاهل فيها الخادم فلتر التصنيف، بدلاً من عرض نفس القائمة لكل تصنيف.
16. تحويل معاينات التصنيفات إلى Mosaic متناثر ومتفاوت الزوايا/الطبقات بدلاً من صف صور تقليدي.
17. إضافة نافذة تأكيد قبل مسح الكاش.
18. تطوير البحث ليجرب البحث العام + movie + series ثم يوحد النتائج ويرتبها بتطابق ذكي: تام، بادئة، احتواء، كلمات مشتركة ومسافة تحرير؛ لتحسين ظهور أسماء مثل From وManifest وLucifer والنتائج المشابهة.
19. استبدال مؤشرات جلب المحتوى الدائرية بـ Skeleton/Shimmer للكروت والقوائم. مؤشرات التقدم المتبقية محددة القيمة فقط لعمليات التنزيل/التقدم الحقيقي.
20. إضافة حالتي Offline وخارج نطاق خوادم إيرثلنك في الرئيسية مع رسالة مناسبة وعرض المحتوى المنزّل محلياً إن وجد.
21. إضافة قسم «حول التطبيق» في مكتبتي بصورة المطور المرفقة، شعار سينماتي، اسم المطور محمد السراي، وصف، وزر موقعنا سكربتاتي المرتبط بـ https://scrptaty.com.

## تحسينات تقنية إضافية

- البحث يحذف التكرارات ويرتب النتائج بدلاً من الاعتماد على استجابة واحدة فقط.
- قراءة الترجمة تدعم `translationFiles` مع fallback إلى `allVideoInfo` وصيغ الحقول القديمة مثل `{name,file}`.
- حفظ `MediaItem` وبيانات التنزيل بصيغة canonical لتقليل فقدان معلومات الموسم/الحلقة بعد إعادة تشغيل التطبيق.
- معاينة السحب تستخدم Player منفصلاً مؤقتاً مع cache لشرائح زمنية، وتلغي الطلبات القديمة عندما يتحرك المستخدم بسرعة.
- عد الحلقة التالية يعتمد على زمن الفيديو نفسه، لذلك لا يسبق التشغيل عند Pause أو Buffering.
- تم توحيد تأثير Blur والحد السفلي للـ Top Bar بين الرئيسية وبقية الصفحات.

## ملاحظة التحقق

تم إجراء فحص ثابت للملفات داخل بيئة العمل: توازن الأقواس والتعليقات/النصوص، الاستيرادات النسبية، مراجع الأصول، تبعيات الحزم وغياب Merge Markers. بيئة التنفيذ الحالية لا تحتوي Flutter/Dart SDK، لذلك لم يتم الادعاء بإجراء `flutter analyze` أو Build فعلي على جهاز. يلزم تشغيلهما في بيئة Flutter النهائية لاختبار المصدر الحقيقي داخل شبكة إيرثلنك.
