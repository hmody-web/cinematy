# ميزات حسابات سينماتي — التنفيذ الحالي

تم دمج هذه النسخة فوق آخر `android.zip` مع آخر تعديلات الواجهة التي سبقتها.

## ما تم تنفيذه داخل التطبيق

- صفحة الحساب بعد تسجيل الدخول أصبحت مختصرة:
  - صورة الحساب والاسم.
  - أيقونة بيانات الحساب أعلى اليسار.
  - معرّف حساب سينماتي منفصل وقابل للنسخ.
  - قسم **مفضلاتي** أسفل المعرّف مباشرة.
- معلومات البريد وطريقة تسجيل الدخول وتاريخ إنشاء الحساب وآخر تسجيل دخول انتقلت إلى نافذة **بيانات الحساب**.
- إنشاء معرّف سينماتي مخصص للمستخدم:
  - 5 إلى 20 حرفاً إنجليزياً أو رقماً.
  - فريد لكل مستخدم.
  - أول اختيار متاح مباشرة.
  - تغييره بعد ذلك مرة واحدة كل 14 يوماً، والتحقق يتم من الخادم وليس من الواجهة فقط.
- المفضلات أصبحت مرتبطة بحساب المستخدم عند تسجيل الدخول:
  - تخزين على الخادم.
  - مزامنة بين الأجهزة.
  - كاش محلي مستقل لكل حساب.
  - طابور مزامنة محلي إذا انقطع الاتصال أثناء إضافة/إزالة مفضلة.
- البحث يحتوي فلتر **مستخدمين**:
  - البحث بالاسم.
  - البحث بمعرّف سينماتي.
  - يتطلب تسجيل الدخول.
- صفحة ملف شخصي للمستخدم:
  - الصورة والاسم والمعرّف.
  - مفضلات المستخدم بنفس بطاقات الأفلام والمسلسلات.
- لا يتم عرض Firebase UID للمستخدم كمعرّف حساب.

## خادم الحساب

العنوان المعتمد داخل التطبيق:

`https://scrptaty.com/apps/cinematy/api/v1`

ملفات الخادم موجودة في ZIP منفصل باسم `cinematy_server_accounts.zip`.

بعد رفع الخادم وربط قاعدة MySQL، نفذ:

```powershell
flutter clean
flutter pub get
flutter run
```

